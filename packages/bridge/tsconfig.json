{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "dist",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true
  },
  "include": ["src"],
  "//": "Tests are run from source by vitest and must not reach dist: package.json ships the whole dist directory, so a compiled test would be published with the connector.",
  "exclude": ["src/**/*.test.ts"]
}
